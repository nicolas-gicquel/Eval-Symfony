{% extends 'base.html.twig' %}

{% block title %}Nouvelle Categorie
{% endblock %}

{% block body %}
	<div class="content">
		<div class="container">
			<h1>Création d'une nouvelle catégorie</h1>

			{{ include('categories/_form.html.twig') }}

			<a href="{{ path('categories_index') }}">Retour à la liste</a>
            <pre>
                {{ dump(app) }}
            </pre>
		</div>
	</div>
{% endblock %}
